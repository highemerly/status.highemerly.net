# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# data files (contain sensitive cache) - DEPRECATED (moved to S3)
/data/

# lambda build artifacts
/lambda/*/node_modules/
/lambda/*/*.zip
/lambda/*/function.zip

# terraform
/terraform/.terraform/
/terraform/.terraform.lock.hcl
/terraform/terraform.tfstate
/terraform/terraform.tfstate.backup
/terraform/terraform.tfvars

# temporary files
/tmp/

# config with secrets - DEPRECATED (credentials moved to SSM Parameters)
# services.json now contains only service definitions and is safe to commit

# stray lambda build artifact
lambda/discord-interaction/ziU0D7pA

# ローカル開発用の生成物（scripts/dev-data.js が作る）
/public/data/
/public/config/
